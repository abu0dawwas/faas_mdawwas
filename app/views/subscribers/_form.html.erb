<div class="form-style-6">
<%= form_with(model: subscriber, html: {id: "subscribe_form"}) do |form| %>
  <% if subscriber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscriber.errors.count, "error") %> prohibited this subscriber from being saved:</h2>

      <ul>
      <% subscriber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <input type="checkbox" name="agree_payment" value="true">
    I agree to pay 5$ per month for you
  </input>
  <br/>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>
